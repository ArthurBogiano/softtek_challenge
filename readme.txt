Para instalar os requisitos de desenvolvimento:

npm install

Para rodar o app em dev:

npm start

O node.js deve estar devidamente instalado e acessível pelo terminal.

O projeto compilado para prod se encontra em ./build/

O projeto em prod só funciona se exposto em um servidor web / vps / hospedagem